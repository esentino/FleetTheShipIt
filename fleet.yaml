namespace: dev
targetCustomizations:
- name: dev
  helm:
    values:
      replication: false

---
namespace: test
targetCustomizations:
- name: test
  helm:
    values:
      replicas: 2

---
namespace: prod
targetCustomizations:
- name: prod
  helm:
    values:
      replicas: 3
